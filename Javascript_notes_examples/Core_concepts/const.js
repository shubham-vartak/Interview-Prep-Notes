// program to calculate area of circle

let radius = 4;
const pi = 3.14; //used const bcoz it should never change.... now if we try to change the value of const it will give error "assigment to constant variable" 
let area = pi * radius * radius;
console.log(area);

// perimeter 
let perimeter = 2 * pi * radius;
console.log(perimeter);